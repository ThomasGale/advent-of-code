{
    "version": "2.0.0",
    "tasks": [
      {
        "type": "shell",
        "label": "build debug",
        "command": "mkdir -p out && cd out && cmake -DCMAKE_BUILD_TYPE=Debug .. && make",
        "options": {
          "cwd": "${workspaceFolder}"
        },
        "problemMatcher": ["$gcc"],
        "group": {
          "kind": "build",
          "isDefault": true
        }
      }
    ]
  }